import React from 'react';

import classes from './CleanStatus.module.css';

const CleanStatus = () => {
   return (
      <div className={classes['clean-status__wrapper']}>
         <label>Clean Status</label>
      </div>
   );
};

export default CleanStatus;
